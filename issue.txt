1. on upload a pdf , it imediately get uploaded and i guess no embeddings are generated and store , as no logs are there in console , so i want that , when user upload a doc , it shows loading screen , while the doc get parsed and embdded and store in the vector store - done

2. even the doc is not stored in the /documents folder under that session-id. - done

3. better chunking and storing with metadata (filename , title , headings of the section), bettern searching instead of semantic and bm25 search . 
ex: if someone search using author or year or title , which is not sementically match with some chunk . 
ex: spelling correction also need to handle 

4. need to improve the planner 
ex: it should plan according to the tools and resourceswe have along with the target to have a good response 

5. needs to check that weather the llm can use multiple tool for a single task or not

6. check how does it reverse or iterate the response generation process

7. i think the docs are not get stored in vector store when uploaded . 

8. needs to mention the sources (url and the external uploaded docs also 'page no if possible')

9. ui shows duplicate stored file name  

10. Evaluation - 
    a. whether the retrieved content actually overlaps with the expected paper section
    b. whether the question is answered by that retrieved content
    c. whether the result is the expected document for that exact question
    d. a stronger measure than only filename matching

11. need to check the combination of different scores

12. duplicate chunk_id in metadata also in main chunk data.

13. remove the eval_cases.jsonl - done

14. check duplicate entry in chroma

15. weather the env variables matches with other files 

16. need to check weather the title , author came from the pdf metadata or ocr

17. _source_quality_check - from tools.py , what it does ?

18. need to check that how does the agent loops response . 

===========================================================================================================================

1. one thing just for information and showcase purpose , can you also write the session in-memory data to a json file , just to show case that we have implimented this thing  

2. in the plan node , where the tasks are generated , i can see that it always generate 5 tasks . rather leve it on llm and provide that not mor than 5 tasks are allowed . check the currect code and update prompt if needed 

